import { NavItemVisibleByRoleDirective } from './nav-item-visible-by-role.directive';

describe('NavItemVisiblebyRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new NavItemVisibleByRoleDirective();
    expect(directive).toBeTruthy();
  });
});
